$.validator.setDefaults({
	highlight : function(e) {
		$(e).closest(".form-group").removeClass("has-success").addClass(
				"has-error")
	},
	success : function(e) {
		e.closest(".form-group").removeClass("has-error").addClass(
				"has-success")
	},
	errorElement : "span",
	errorPlacement : function(e, r) {
		e.appendTo(r.is(":radio") || r.is(":checkbox") ? r.parent().parent()
				.parent() : r.parent())
	},
	errorClass : "help-block m-b-none",
	validClass : "help-block m-b-none"
}), $().ready(function() {
	$("#commentForm").validate();
	var e = "<i class='fa fa-times-circle'></i> ";
	$("#signupForm").validate({
		rules : {
			roleName : "required",
			sortOrder : {
				required : !0,
				maxlength : 3
			}
		},
		messages : {
			sortOrder: {
				required: e + "请输入序号",
				maxlength: e + "序号最大3位数字"
			},
			roleName : e + "请输入角色名称"
			
		}
	}), $("#sortOrder").focus(function() {
		var e = $("#roleName").val();
		e && !this.value && (this.value = e + ".")
	})
});